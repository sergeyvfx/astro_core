This directory contains leap second and earth orientation parameters tables
provided by IERS. These tables are only used for regression tests and are not
included into the final library. This is because those tables are to be
updated o na regular basis.
